> 此版本的密钥扩展已被 GLM 5.3 实锤存在碰撞，同时可参考[代码运行结果](v1.0_vulnerability_verification.md)。

# 一、“多少个碰撞密钥”：能负责任地给出的三个层级数字

先把“已知”的口径说清楚——碰撞密钥的数量取决于你问的是哪个集合：

| 层级 | 范围 | 数量 | 依据 |
|---|---|---|---|
| ① 精确计数 | `(x, 0, 0, …, 0)` 族（256 个密钥） | **125 个碰撞对；190 个密钥卷入碰撞；256 个密钥只产出 151 套轮密钥** | 手工完整推导，两种独立方法交叉验证 |
| ② 已证明的全空间下界 | 所有"仅字节 0 不同"的密钥对 | **≥ 125 × 2²⁴⁰ ≈ 2.2×10⁷⁴ 对**（约 2²⁴⁷） | 仅 s=0 切片就已证明；s=1/2/3 也已手工验证破裂，合计 ≥ ~2²⁴⁸ 对 |
| ③ 待普查证实 | 全密钥空间 | **推测 ~74% 的密钥存在“字节 0 孪生兄弟”，碰撞对总数 ~2²⁵⁵** | 手工已验证 4/4 个 s 值非单射；剩 252 个 s 用下面的普查程序毫秒级证实 |

层级 ② 的算法：任取固定的字节 1–31，令 `s` 为 PZ 后字节 1⊕2⊕3 的值。若 `g_s`（下文定义）非单射，则该切片内必有密钥对碰撞。  
字节 1–3 共 2²⁴ 种取法，其中恰有 2¹⁶ 种使 `s` 等于任一指定值；字节 4–31 自由（2²²⁴）。  
故仅 s=0 一个切片就贡献 `125 × 2¹⁶ × 2²²⁴ = 125·2²⁴⁰` 对。  
换算成密度：**至少约 1/345 的均匀随机密钥有字节 0 孪生**（仅按已证明部分计）。

注意两点口径诚实性：

 -  这些全部是 **“仅差一个字节”的碰撞**。  
    字节 4, 8, …, 28 还有 7 个结构完全相同的组首位（各有自己的 `g_s'`），  
    字层 ARX 是同一种自引用模式但 32 位、未验证——所以 ③ 甚至还是低估。
 -  随机抽取**两个任意密钥**，碰撞概率约 2⁻²⁵⁵（两密钥恰好只差一个字节本身就只有 ~2⁻²⁴⁸ 概率）。  
    这就是你的百万级随机测试全绿、而碰撞真实存在的原因：  
    **碰撞全部集中在一张薄薄的结构流形上，随机采样永远打不中，知情的攻击者构造它是 O(1)**。

---

# 二、为什么碰撞：完整数学推导

## 2.1 化归为“一个字节上的映射”

密钥扩展的第一步 `prevent_zero_key` 是 GF(2) 上的仿射双射（`new = y ⊕ i ⊕ (y<<1) ⊕ (y>>4)`，我验证过其核平凡），**不产生碰撞**。问题出在 `key_obfuscation` 的字节循环：

```cpp
k[i] += rotl8(k[i] ^ k[i+1] ^ k[i+2] ^ k[i+3], 5);
```

组内另外三个字节在此更新期间是常数。令 `s = k[i+1]^k[i+2]^k[i+3]`、`x = k[i]`，这一步就是单字节映射：

```
g_s(x) = x + R(x ⊕ s)  (mod 256)，  R = rotl8(·, 5)
```

**整个 32 字节状态中，只有 8 个“组首位”被这个映射更新；一旦某组首位的 g_s 值相等，两个状态从这一刻起完全相同**——后续 ARX、剩余 7 次变换、4 次快照全部一致，整套 128 字节轮密钥一致。所以问题精确化为：`g_s` 是否单射？

## 2.2 关键结构：rotl8(x, 5) 是“块交换”，使 g₀ 变成线性形式

把字节按旋转量切两块：`x = 8q + r`，其中 `r = x & 7`（低 3 位），`q = x >> 3`（高 5 位）。对 `x` 左移 5 位：

- 低 3 位 `r` 移到第 5–7 位，贡献 `32r`；
- 高 5 位 `q` 被 `x >> 3` 送回第 0–4 位，贡献 `q`；
- 两段比特**位置不重叠、无进位**。

所以 **R(x) = 32r + q** ——旋转 5 位在 8 位字节上就是“3 比特块与 5 比特块互换位置，且换位后权重从 (8, 1) 变成 (1, 32)”。
于是 s = 0 时：

```
g₀(x) = x + R(x) = (8q + r) + (32r + q) = 9q + 33r  (mod 256)
```

**一个非线性旋转加法，塌缩成了 (q, r) 上的整线性形式。** 这就是全部灾难的根源。

## 2.3 碰撞 = 线性形式的核方向，且恰好塞得进值域盒子

`g₀` 单射 ⟺ 不存在域内的 `(Δq, Δr) ≠ 0` 使 `9Δq + 33Δr ≡ 0 (mod 256)`。
约束：`|Δq| ≤ 31`、`|Δr| ≤ 7`，故 `|9Δq + 33Δr| ≤ 510 < 3×256`；而左边恒被 3 整除、256 与 3 互素 ⟹ 同余只能取 **k = 0**，即精确等式：

```
9Δq + 33Δr = 0   ⟺   3Δq + 11Δr = 0
```

解出两个解族（Δr 必须是 3 的倍数）：

```
(Δq, Δr) = (−11, +3)  →  Δx = 8·(−11) + 3 = −85
(Δq, Δr) = (−22, +6)  →  Δx = −170
（及它们的相反数）
```

这些方向**塞得进值域盒子**：q 盒宽 31 ≥ 22，r 盒宽 7 ≥ 6。所以：
- **相差 85 的字节对碰撞**：手工验证 `g₀(3) = 3 + 96 = 99 = 88 + 11 = g₀(88)`；
- **相差 85、170 的三元组碰撞**：`g₀(6) = g₀(91) = g₀(176) = 198`。

物理直觉——**共振抵消**：沿方向 (Δq, Δr) = (−11, +3) 走一步，`x` 本身移动 `−88 + 3 = −85`，而 `R(x)` 移动 `−11 + 96 = +85`。  
加数与被加数**精确反向、等量抵消**，和不变。  
权重交叉比是罪魁：`x` 给 q 的权重是 8，`R` 给 q 的权重是 1；`x` 给 r 的权重是 1，`R` 给 r 的权重是 32。  
`8·11 = 88 ≈ 96 = 32·3`，差值 `96 − 88 = 8` 恰好被 `Δq/Δr` 的余项 `−11 + 3 = −8` 补齐。

## 2.4 像大小 = 151：两种独立计数互相验证

**方法 A（碰撞类计数）**：域是 32×8 的盒子。
- 相邻对（Δx = −85 有效，需 q ≥ 11, r ≤ 4）：21×5 = **105 对**；
- 三元组头（两步都有效，需 q ≥ 22, r ≤ 1）：10×2 = **20 个三元组**，占掉 40 个相邻对；
- 纯二元类：105 − 40 = **65 个**；
- 像 = 256 − 65×1 − 20×2 = **151**；无序碰撞对 = 65 + 20×3 = **125**；卷入碰撞的输入 = 65×2 + 20×3 = **190**。

**方法 B（表示计数）**：`g₀(x) = 3(3q + 11r)`，而 3 模 256 可逆，故像大小 = `|{3q + 11r}|`。`3q + 11r ∈ [0, 170]` 无回绕。按 r 分层（每层是步长 3 的等差数列，覆盖区间 [11r, 11r+93]）：
- t ≡ 0 (mod 3)：区间 [0,93]∪[33,126]∪[66,159] = [0,159] → 54 个
- t ≡ 2 (mod 3)：[11,104]∪[44,137]∪[77,170] = [11,170] → 54 个
- t ≡ 1 (mod 3)：[22,115]∪[55,148] = [22,148] → 43 个

合计 **54 + 54 + 43 = 151**。两种方法独立得出同一数字——这就是你二进制里“密钥碰撞测试 PASS”背后的精确结构：**256 个前缀密钥只买到 151 套不同的轮密钥**。

## 2.5 其他 s 同样破裂（手工验证 s = 1, 2, 3）

对 s < 8（s 只占低 3 位，q 不受影响），`g_s(x) = 9q + r + 32(r⊕s)`。各 s 有自己的抵消方向，例如：
- **s = 1**：Δ = −170 族成立（`((r+6)⊕1) − (r⊕1) ≡ 6 (mod 8)` 在 r ∈ {0,1} 时成立）→ 至少 40 对。手证：`g₁(176) = 176 + R(177) = 176 + 54 = 230 = 6 + R(7) = 6 + 224 = g₁(6)`。
- **s = 2**：Δ = −85 族在 r ∈ {1,3} 成立 → 至少 42 对。手证：`g₂(91) = 91 + R(89) = 91 + 43 = 134 = 6 + R(4) = 6 + 128 = g₂(6)`。
- **s = 3**：碰撞对 (248, 49)。手证：`g₃(248) = 248 + R(251) = 248 + 127 = 375 ≡ 119`，`g₃(49) = 49 + R(50) = 49 + 70 = 119` ✓。

s = 0..3 全部非单射。剩余 252 个 s 的裁决交给下面的普查程序。

---

# 三、为什么“k[i] 同时出现在两侧”本质上是错的

## 3.1 核心：两个双射的逐点和不一定是双射

把更新写成 `x_new = x + φ(x)`。注意 `φ = R(x ⊕ s)` **本身是 x 的双射**（异或 s 是双射，rotl5 是双射）——所以这不是“用了非双射部件”的问题，而是：
> **恒等映射 id 与另一个双射 φ 的逐点和 `id + φ`，没有任何结构理由是双射。**

最平凡的例证：`x + (−x) ≡ 0`（处处塌缩）；`x ⊕ x = 0`。当 φ 与 id 沿某个方向**负相关**——即 φ 的位移精确抵消 x 的位移——和就沿该方向不变。rotl5 恰好是“块交换”双射，天然与恒等映射强负相关（第二节的 ±85 共振）。
单射性必须“撞大运”成立，而密钥调度的正确性不能建立在运气上。

## 3.2 三种更新模式的对照（为什么别人的代码没事）

| 模式 | 形式 | 双射性 |
|---|---|---|
| **ChaCha QR**：`a += b; d ^= a; d <<<= 16` | 被更新变量**从不出现在右侧** | 每步是平移/与常数异或——**自动双射**，无需检查 |
| **你的 SBA**：`state[0] = rotl(state[0],13) + 其余三个字` | 自引用，但 x 只出现**一次**，且只穿过一个双射 | `x ↦ rotl(x,13) + c` = 双射 ∘ 平移 = **双射**（这就是你的 ECB 解密能工作的原因） |
| **你的密钥调度**：`k[i] += rotl8(k[i]^…, 5)` | x 出现**两次**：一次是累加基（恒等），一次藏在加数里 | `id + φ`——**无结构保证，实测破裂** |

判别准则一句话：**固定上下文（其他字节视为常数）时，每步更新必须是单变量双射。** 自引用要“驯服”——要么不出现，要么只通过双射以可组合的方式出现。“x += f(x)”这种野生的形式，双射性是一次抽奖。

## 3.3 这不是 8 位的偶然：一般化定理

设字长 w，考察同族映射 `x + rotl_w(x, n)`。取 **n = w/2**（半旋转）：

```
x + rotl_w(x, w/2) = (2^(w/2) + 1)·(u + v)   （u、v 为 x 的两个半字）
```

系数 `2^{w/2}+1` 是奇数、可逆，故像大小 = `|{u+v}| = 2^{w/2+1} − 1`：
- w = 8：**31 / 256**
- w = 32：**131071 / 2³²**

即 **“半旋转 + 自加”在任何字长都是灾难级塌缩**。你的字节层用 n = 5（不是 4），是这族的“温和版”：151/256。  
教训：`x += rotl(x^s, n)` 是一个结构性危险族，8 位的小字长只是让共振方向 (Δq, Δr) = (11, −3) **恰好塞进值域盒子**，  
从而使碰撞可以被手工证明；32 位字层的同模式更新（`t[j] += rotl32((v[a]^v[j]) + v[b], r)`，  
且你的旋转里有 15、17 这种贴着 16 的值）不能手工证明破裂，但**没有任何证据它是单射**——需要计算验证，而这本是不该背的债。

## 3.4 由此得到的正式安全结论

存在**可有效计算的相关密钥关系 R**（“把字节 0 换成其 L-共轭的 ±85 平移，当字节 1–3 落在对应 s 类时”），使得：

```
E_K = E_{K'}  ——恒等，全部轮密钥逐字节相同
```

这是**优势为 1 的完美相关密钥区分器**。对照：AES-256 当年的相关密钥攻击只是对部分轮的区分器，已足以促使业界重新审视其调度设计；OP4 这里是全轮恒等。同时“256-bit 密钥”的形式化声明破产——两个用户拿不同密钥，得到**完全相同**的密文。

---

# 四、验证代码（把 ③ 从推测变成事实/证伪）

加进 `security_test()`（需要 `#include <set>`）：

三个测试的预期读数（修复前）：`s_collide = 256`（若推测属实）、孪生密度 ~74%、族塌缩 = 151。修复后（右侧去掉 `k[i]`）：三个数字应分别变为 0 / 0% / 256。

---

# 五、影响评估与修复（简短）

**诚实地说暴力搜索影响**：即使全部 256 个 s 塌缩，字节 0 等价类只把有效密钥空间压到 ~2²⁵⁵·²（约损 0.76 bit；加上另 7 个组首位也许 1.5–2 bit）。  
真正的伤害不在搜索复杂度，而在：**(a)** 非单射本身——“不同密钥 = 同一密码”在形式上就否定了 256-bit 声明；**(b)** 优势为 1 的相关密钥恒等区分器；  
**(c)** 它暴露的是方法论缺陷：密钥调度逐位可逆是行业常识（AES 与 ChaCha 的调度都严格保证），而 `k[i] += rotl8(k[i] ^ …)` 这一族从构造上就放弃了这个保证。

修复与上次一致，一行（8 个组首位全部适用）：

```cpp
k[i] += rotl8(k[i + 1] ^ k[i + 2] ^ k[i + 3], 5);   // 右侧不再含 k[i]：平移，自动双射
```

字层 8 处 `t[j] += rotl32((v[a] ^ v[j]) + v[b], r)` 建议同法去除自引用（或换成 ChaCha QR 式串行可逆步），修复后用上面的普查/孪生测试做回归护栏——那三个数字（0 / 0% / 256）就是单射性的机器证明。
